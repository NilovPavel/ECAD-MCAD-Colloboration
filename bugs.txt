Спецификация
1 В некоторых случаях в переменных данных неверно производится сортировка из разных исполнений.
2 Нет информации по жгутам.
2.1 Нет сортировки материалов из жгутов
2.2 Подумать как идентифицировать сам провод/кабель на чертеже.
3 Возникает ошибка, если имя исполнения = null
4 Возникает ошибка установки позиции в случае, если обозначение компонентов одинаковое, а наименования разные
Пример: D:\Test\BoardData\ReportData\ИЮТВ.468454.009.xml
5 

CadBoardData:
1. Не у всех элементов есть корректный дезигнатор(BK?/A5:3). Данная ситуация не является корректной.
Решение: 
а) Выбрасывать данные элементы из коллекций варианта.
б) Выдавать окно с ошибкой.
в) Принудительно записывать свободный индекс в дезигнатор в отчете.
г) Принудительно записывать свободный индекс в дезигнатор в проекте.

2. Подумать, стоит ли записывать координаты (X,Y,Z) в XML-файл, если отсутствует плата.
Решение:
а) Записывать пустые.
б) Создать строителя который будет формировать XML с координатами только для построения сборки 3d.

3. Не у всех компонентов будут параметры ЕКСД, в т.ч. и альтиумовских.
а) Подставлять импровизированные данные.
Комментарий: Практика показывает, что в случае со сбоками солида это работает неплохо.
б) Подставлять данные из альтернативных полей.
в) Пропускать их при записи xml.
г) Выдавать окно о том, что данные не полноценные и прекращать работу.
д) Вставлять "пустые" данные / данные по-умолчанию.

+4. Создать componentCAD из платы.

6. Плата есть и в "геометрии платы" и в вариантах (как быть при передаче ECAD=>MCAD + PDM).

-7. Как быть с материалами, которые представлены телами в Solidworks (нужны поля "Количество" и "Единицы измерения")?
а) Добавить в SWE PDM соответствующие поля.
б) Импровизация данных - хз как.
в) "Переводить" в штуки
г) Попытаться из контекста детали понять, что за материал
Комментарий: нужен гуру.

+8. SW. "Кабель^..." записывается в коллекцию варианта.
Решение: найти кабель по признаку и удалить его из коллекции.
Признаки косвенные:
а) Сборка является жгутом - обязательное условие
б) Все остальные компоненты имеют дезигнатор - отчасти подходит :), тут исходим из технологии
в) Кабель имеет значок каретки в названии(не всегда) - не подходит :(
г) Кабель имеет тип swRouteComponentTypeID_e.swRouteCompType_Unknown - другой компонент тоже может иметь этот тип, так что не подходит :(
д) все остальные типы имеют тип swRouteComponentTypeID_e.swTube - не всегда, потому не подходит :( 